import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MARB Solutions | Digital Solutions for Enterprises",
  description: "MARB Solutions delivers cutting-edge digital solutions — from web and mobile development to AI integration, cloud architecture, and enterprise transformation.",
  keywords: ["MARB Solutions", "digital transformation", "web development", "mobile apps", "AI integration", "cloud solutions", "enterprise software"],
  authors: [{ name: "MARB Solutions" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}